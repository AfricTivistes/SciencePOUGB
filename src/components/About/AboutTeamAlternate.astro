---
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";
import AcademicMember from "./AcademicMember.astro";
import { academicMembers } from "@data/academicTeam.js";
---

<section class="py-16 bg-gray-50 dark:bg-gray-900">
  <div class="container mx-auto px-4">
    <div class="max-w-3xl mx-auto text-center mb-12">
      <span class="inline-block px-3 py-1 text-sm font-medium bg-primary-100 text-primary-800 dark:bg-primary-900 dark:text-primary-300 rounded-full mb-4">Nos Encadrants</span>
      <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">Encadrement Académique</h2>
      <div class="w-20 h-1 bg-primary-600 mx-auto mb-4"></div>
      <p class="text-lg text-gray-600 dark:text-gray-300">
        Notre club bénéficie du soutien et de l'expertise d'un groupe d'enseignants-chercheurs dévoués, qui guident les étudiants dans leurs projets académiques et professionnels.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto">
      {academicMembers.map((member) => (
        <AcademicMember 
          name={member.name}
          role={member.role}
          image={member.image}
          description={member.description}
          socialLinks={member.socialLinks}
        />
      ))}
    </div>
  </div>
</section>